{
  "name": "my-gulp-project",
  "version": "1.0.0",
  "description": "Gulp 4 Setup",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "bundle exec jekyll clean",
    "build": "bundle exec jekyll build --config _config.yml --incremental"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "browser-sync": "^2.26.7",
    "gulp": "^4.0.2",
    "gulp-concat": "^2.6.1",
    "gulp-concat-css": "^3.1.0",
    "gulp-postcss": "^8.0.0",
    "gulp-sass": "^4.1.0"
  },
  "dependencies": {
    "cross-spawn": "^7.0.3",
    "gulp-uglify": "^3.0.2"
  }
}
